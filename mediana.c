//mediana sem ordenar 
//calcule o desvio padrao
//fibbonacci

#include <stdio.h>

void mediana(int vet[], unsigned tamVet) {
	unsigned cont;
	int mediana, maior, menor;
	
	if(tamVet%2 != 0) { //vetor impar
		maior = vet[0];
		
		for(cont = 0; cont < tamVet; cont++) {
			if(vet[cont] > maior) {
				
			}
		}
	
	}
	
}

int main() {
	int vet[30];
	
	scanf("%d%*c", vet[], 30);
	mediana(vet, 30);
}
